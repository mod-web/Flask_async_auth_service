
---
post:
  summary: Adds or deletes user role
  description: >
    Ruteruns updates user rolse for success
  tags:
    - Roles
  parameters:
    - name: user_id
      in: json
      required: true
      schema:
        type: string
    - name: role
      in: json
      required: true
      schema:
        type: string
    - name: action
      in: json
      required: true
      schema:
        type: string
    - name: access_token_cookie
      in: cookie
      required: true
      schema:
        type: string
    - name: refresh_token_cookie
      in: cookie
      required: true
      schema:
        type: string
  responses:
    200:
      description: 'Ok'